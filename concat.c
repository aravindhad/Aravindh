#include<stdio.h>

void main(void)
{
  char str1[25],str2[25];
  int m=0,n=0;
  printf("\nEnter First String:");
  gets(str1);
  printf("\nEnter Second String:");
  gets(str2);
  while(str1[m]!='\0')
  i++;
  while(str2[n]!='\0')
  {
    str1[m]=str2[n];
    m++;
    n++;
  }
  str1[m]='\0';
  printf("\nConcatenated String is %s",str1);
}
